# HTML
# Archive your static HTML project and save it with the build record.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool: Default

steps:
- task: CopyFiles@2
  inputs:
    SourceFolder: '$(Agent.BuildDirectory)/s/config'
    Contents: '**'
    TargetFolder: '/var/www'

